---
// Importamos o componente otimizado do Astro
import { Picture } from 'astro:assets';
// Importamos o ícone leve do Lucide
import { ArrowRight, CheckCircle2 } from 'lucide-react';

// Importação ESTÁTICA da imagem local (o Astro deteta o ficheiro fisicamente)
import heroImage from '../assets/hero.jpg'; 
---

<section class="relative pt-24 pb-12 lg:pt-32 lg:pb-24 overflow-hidden bg-neutral-50 dark:bg-neutral-900 transition-colors duration-300">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-8">
      
      <div class="w-full lg:w-1/2 flex flex-col items-center lg:items-start text-center lg:text-left animate-fade-in-up">
        
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-primary-50 dark:bg-primary-900/30 text-primary-600 dark:text-primary-500 text-sm font-semibold mb-6">
          <span class="flex h-2 w-2 rounded-full bg-primary-600 animate-pulse"></span>
          Boilerplate V1.0 Lançado
        </div>

        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-neutral-900 dark:text-white leading-tight mb-6">
          Crie Landing Pages com <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary-600 to-accent">Performance Extrema</span>
        </h1>
        
        <p class="text-lg sm:text-xl text-neutral-600 dark:text-neutral-300 mb-8 max-w-2xl">
          Acelere as suas campanhas com a arquitetura que os gigantes usam. LCP inferior a 1.2s, conversão otimizada e design de classe mundial.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 w-full sm:w-auto mb-8">
          <a href="#contact" class="inline-flex justify-center items-center px-8 py-4 bg-primary-600 hover:bg-primary-700 text-white font-bold rounded-lg transition-all transform hover:scale-105 shadow-lg hover:shadow-primary-500/30 group">
            Quero Aumentar as Vendas
            <ArrowRight className="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform" />
          </a>
        </div>

        <div class="flex items-center gap-4 text-sm text-neutral-500 dark:text-neutral-400 font-medium">
          <div class="flex items-center gap-1">
            <CheckCircle2 className="w-4 h-4 text-secondary" /> Zero JS Overhead
          </div>
          <div class="flex items-center gap-1">
            <CheckCircle2 className="w-4 h-4 text-secondary" /> Lighthouse 100
          </div>
        </div>
      </div>

      <div class="w-full lg:w-1/2 relative group">
        <div class="absolute -inset-1 bg-gradient-to-r from-primary-600 to-accent rounded-2xl blur opacity-25 group-hover:opacity-40 transition duration-1000 group-hover:duration-200"></div>
        
        <Picture 
          src={heroImage} 
          formats={['avif', 'webp']} 
          alt="Dashboard demonstrando a alta performance da aplicação"
          loading="eager" 
          fetchpriority="high"
          class="relative rounded-2xl shadow-2xl w-full h-auto object-cover transform transition-transform duration-700 hover:scale-[1.01]"
        />
      </div>

    </div>
  </div>
</section>